import React from "react";
import Navbar from "../components/Navbar/Navbar";

const Stages = () => {
     return(
        <div>
            <Navbar />
            Stages
        </div>
    );
};

export default Stages;